# Docker Compose reads this root-level file for variable substitution.
# Copy to `.env` and set your real values before running `docker compose up`.

OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini
